# Utilities

```@meta
CurrentModule = PEPit
```

These helpers support the symbolic linear-combination representation used by
[`Point`](@ref) and [`Expression`](@ref). Most users interact with them
indirectly through overloaded arithmetic.

```@docs
merge_dicts
multiply_dicts
prune_dict
get_block
get_nb_blocks
get_is_leaf
```
